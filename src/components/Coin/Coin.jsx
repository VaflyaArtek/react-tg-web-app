import React from 'react';
import './Coin.css'

const Coin = (props) => {
    return (
        // <button {...props} className={'button' + props.className}/>
        <div>dsad</div>
    );
};

export default Coin;